/*
 * Camel Api Route test generated by camel-api-component-maven-plugin
 * Generated on: Wed Sep 11 15:12:02 CEST 2019
 */
package org.apache.camel.component.google.calendar;

import java.util.HashMap;
import java.util.Map;

import org.apache.camel.builder.RouteBuilder;
import org.junit.Ignore;
import org.junit.Test;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.apache.camel.component.google.calendar.internal.GoogleCalendarApiCollection;
import org.apache.camel.component.google.calendar.internal.CalendarChannelsApiMethod;

/**
 * Test class for {@link com.google.api.services.calendar.Calendar$Channels} APIs.
 * TODO Move the file to src/test/java, populate parameter values, and remove @Ignore annotations.
 * The class source won't be generated again if the generator MOJO finds it under src/test/java.
 */
public class CalendarChannelsIntegrationTest extends AbstractGoogleCalendarTestSupport {

    private static final Logger LOG = LoggerFactory.getLogger(CalendarChannelsIntegrationTest.class);
    private static final String PATH_PREFIX = GoogleCalendarApiCollection.getCollection().getApiName(CalendarChannelsApiMethod.class).getName();

    // TODO provide parameter values for stop
    @Ignore
    @Test
    public void testStop() throws Exception {
        // using com.google.api.services.calendar.model.Channel message body for single parameter "contentChannel"
        final com.google.api.services.calendar.Calendar.Channels.Stop result = requestBody("direct://STOP", null);

        assertNotNull("stop result", result);
        LOG.debug("stop: " + result);
    }

    @Override
    protected RouteBuilder createRouteBuilder() throws Exception {
        return new RouteBuilder() {
            public void configure() {
                // test route for stop
                from("direct://STOP")
                    .to("google-calendar://" + PATH_PREFIX + "/stop?inBody=contentChannel");

            }
        };
    }
}
